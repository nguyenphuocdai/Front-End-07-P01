function CourseList() {
    this.listCourse = [];
    // this.AddUser = function (elm) {
    //    this.DSND.push(elm);
    // }
    // this.DeleteUser = function (account) {
    //    var index = ListUser.searchUserByAccount(account);
    //    this.DSND.splice(index, 1);
    // }
    // this.EditUser = function (elm) {
    //    var index = this.searchUserByAccount(elm.TaiKhoan);
    //    var old_user = this.DSND[index];
    //    old_user.MatKhau = elm.MatKhau;
    //    old_user.HoTen = elm.HoTen;
    //    old_user.Email = elm.Email;
    //    old_user.SoDT = elm.SoDT;
    // }
    // this.searchUser = function (str) {
    //    str = str.toLowerCase().trim();
    //    if(str == ""){
    //       return this.DSND;
    //    } else {
    //       var search_string_list = new UserList();
    //       for(var i = 0; i< ListUser.DSND.length; i++){
    //          var user = ListUser.DSND[i];
    //          if(user.HoTen.search(str) !== -1){
    //             search_string_list.DSND.push(user);
    //          }
    //       }
    //          return search_string_list.DSND;
    //    }
    // }
    // this.searchUserByAccount = function (acount) {
    //    for(var i = 0; i < this.DSND.length; i++){
    //       if(acount === this.DSND[i].TaiKhoan){
    //          return i;
    //       }
    //    }
    // }
 }